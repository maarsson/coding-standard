name: Validate coding standards

on:
  push:
  pull_request:

jobs:
  phpmd:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        php: ['8.4', '8.5']

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          tools: composer

      - name: Install dependencies
        run: composer install --no-interaction --prefer-dist

      - name: Validate PHPMD ruleset file exists
        run: test -f resources/phpmd.xml.dist

      - name: PHPMD should pass for GoodCode
        run: composer ci:phpmd-passes

      - name: PHPMD should fail for BadCode (expected)
        run: |
          set +e
          composer ci:phpmd-fails
          exit_code=$?
          set -e
          if [ $exit_code -eq 0 ]; then
            echo "Expected PHPMD to fail for BadCode fixtures, but it passed."
            exit 1
          fi

  phpcs:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        php: ['8.4', '8.5']

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          tools: composer

      - name: Install dependencies
        run: composer install --no-interaction --prefer-dist

      - name: Validate PHPCS ruleset file exists
        run: test -f resources/phpcs.xml.dist

      - name: PHPCS should pass for GoodCode
        run: composer ci:phpcs-passes

      - name: PHPCS should fail for BadCode (expected)
        run: |
          set +e
          composer ci:phpcs-fails
          exit_code=$?
          set -e
          if [ $exit_code -eq 0 ]; then
            echo "Expected PHPCS to fail for BadCode fixtures, but it passed."
            exit 1
          fi

  phpcsfixer:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        php: ['8.4', '8.5']

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          tools: composer

      - name: Install dependencies
        run: composer install --no-interaction --prefer-dist

      - name: Validate PHP-CS-FIXER ruleset file exists
        run: test -f resources/php-cs-fixer.php.dist

      - name: PHP-CS-FIXER should pass for GoodCode
        run: composer ci:phpcsfixer-passes

      - name: PHP-CS-FIXER should fail for BadCode (expected)
        run: |
          set +e
          composer ci:phpcsfixer-fails
          exit_code=$?
          set -e
          if [ $exit_code -eq 0 ]; then
            echo "Expected PHP-CS-FIXER to fail for BadCode fixtures, but it passed."
            exit 1
          fi

  consumer-smoke:
    name: Consumer smoke test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          tools: composer

      - name: Install repo dependencies (optional)
        run: composer install --no-interaction --prefer-dist

      - name: Prepare path repo for consumer test
        run: |
          rm -rf /tmp/coding-standard-consumer-smoke
          mkdir -p /tmp/coding-standard-consumer-smoke/repo
          rsync -a --delete ./ /tmp/coding-standard-consumer-smoke/repo/

      - name: Run consumer smoke test
        working-directory: /tmp/coding-standard-consumer-smoke
        run: |
          /tmp/coding-standard-consumer-smoke/repo/test/ci/consumer-smoke.sh
